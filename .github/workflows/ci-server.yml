name: CI - Server

on:
    push:
        branches: [ main, master ]
        paths:
            - 'server/**'
            - '.github/workflows/ci-server.yml'
    pull_request:
        branches: [ main, master ]
        paths:
            - 'server/**'
            - '.github/workflows/ci-server.yml'

jobs:
    server:
        name: Server (Go ${{ matrix.go-version }}, PG ${{ matrix.postgres-version }})
        runs-on: ubuntu-latest
        strategy:
            matrix:
                go-version: ['1.26.2']
                postgres-version: ['14', '15', '16', '17', '18']
            fail-fast: false

        services:
            postgres:
                image: postgres:${{ matrix.postgres-version }}
                env:
                    POSTGRES_PASSWORD: postgres
                    POSTGRES_DB: postgres
                options: >-
                    --health-cmd pg_isready
                    --health-interval 10s
                    --health-timeout 5s
                    --health-retries 5
                ports:
                    - 5432:5432

        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Go ${{ matrix.go-version }}
              uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
              with:
                  go-version: ${{ matrix.go-version }}
                  cache: false

            - name: Cache Go modules
              uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
              with:
                  path: |
                      ~/go/pkg/mod
                      ~/.cache/go-build
                  key: ${{ runner.os }}-go-${{ matrix.go-version }}-server-${{ hashFiles('server/src/go.sum') }}
                  restore-keys: |
                      ${{ runner.os }}-go-${{ matrix.go-version }}-server-

            - name: Verify dependencies
              working-directory: server/src
              run: go mod verify

            - name: Download dependencies
              working-directory: server/src
              run: go mod download

            - name: Lint server code
              if: matrix.go-version == '1.26.2' && matrix.postgres-version == '18'
              working-directory: server
              run: |
                  go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest
                  echo "$(go env GOPATH)/bin" >> $GITHUB_PATH
                  make lint

            - name: Run go vet
              if: matrix.go-version == '1.26.2' && matrix.postgres-version == '18'
              working-directory: server/src
              run: go vet ./...

            - name: Check formatting
              if: matrix.go-version == '1.26.2' && matrix.postgres-version == '18'
              working-directory: server
              run: |
                  gofmt -l ./src > /tmp/gofmt.out
                  if [ -s /tmp/gofmt.out ]; then
                    echo "The following files are not formatted:"
                    cat /tmp/gofmt.out
                    exit 1
                  fi

            - name: Build server
              working-directory: server
              run: make build

            - name: Verify binary
              run: |
                  ls -lh server/ai-dba-server
                  file server/ai-dba-server

            - name: Wait for PostgreSQL
              run: |
                  until pg_isready -h localhost -p 5432 -U postgres; do
                    echo "Waiting for PostgreSQL..."
                    sleep 2
                  done

            - name: Run tests with coverage
              working-directory: server
              env:
                  TEST_AI_WORKBENCH_SERVER: postgres://postgres:postgres@localhost:5432/postgres
              run: make coverage

            - name: Generate coverage report
              if: matrix.go-version == '1.26.2' && matrix.postgres-version == '18'
              working-directory: server/src
              run: |
                  go tool cover -func=coverage.out

            - name: Upload build artifact
              if: matrix.go-version == '1.26.2' && matrix.postgres-version == '18'
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
              with:
                  name: ai-dba-server-linux-amd64
                  path: server/ai-dba-server
                  retention-days: 7

            - name: Upload coverage artifact
              if: matrix.go-version == '1.26.2' && matrix.postgres-version == '18'
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
              with:
                  name: server-coverage
                  path: |
                      server/src/coverage.html
                      server/src/coverage.out
                  retention-days: 7
